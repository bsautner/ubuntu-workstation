#!/bin/bash
echo "backup.sh backup home to usb"
echo "git-hr hard reset from master"
echo "git-m merges current work to a uuid branch, then checks out master to a new uuid branch"
echo "git-nb creates a new branch from where you are and pushes it to origin"
echo "git-push adds a commit message and pushes to the current branch"

